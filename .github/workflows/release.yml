name: Build and Deploy Release

on:
  push:
    tags:
      - 'v*.*.*'  # Triggers on version tags like v1.2.3
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment for ACR (demo, staging, production)'
        required: false
        default: 'production'
        type: choice
        options:
          - demo
          - staging
          - production
      version:
        description: 'Version to release (e.g., 1.2.3)'
        required: false
        type: string

env:
  ACR_ENVIRONMENT: ${{ github.event.inputs.environment || 'production' }}
  ACR_REGISTRY: reliabilitydemoacr${{ github.event.inputs.environment || 'production' }}.azurecr.io
  STAGING_WEB_IMAGE_NAME: sixeyed-staging/reliability-demo
  STAGING_WORKER_IMAGE_NAME: sixeyed-staging/reliability-demo-worker
  PRODUCTION_WEB_IMAGE_NAME: sixeyed/reliability-demo
  PRODUCTION_WORKER_IMAGE_NAME: sixeyed/reliability-demo-worker

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Extract version from tag
      id: version
      run: |
        # Extract version from tag (remove 'v' prefix) or use manual input
        if [ -n "${{ github.event.inputs.version }}" ]; then
          VERSION=${{ github.event.inputs.version }}
        else
          VERSION=${GITHUB_REF#refs/tags/v}
        fi
        echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
        echo "Building version: $VERSION"

    - name: Get commit SHA from merge commit
      id: commit
      run: |
        # For release tags, we want to promote images from the most recent merge commit
        # This should be the staging images that were built when the PR was merged
        COMMIT_SHA=$(git log --format="%H" -n 1 --merges)
        if [ -z "$COMMIT_SHA" ]; then
          # If no merge commit found, use the current commit
          COMMIT_SHA=${{ github.sha }}
        fi
        echo "COMMIT_SHA=$COMMIT_SHA" >> $GITHUB_OUTPUT
        echo "Using commit: $COMMIT_SHA"

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: |
          {
            "clientId": "${{ secrets.AZURE_CLIENT_ID }}",
            "clientSecret": "${{ secrets.AZURE_CLIENT_SECRET }}",
            "subscriptionId": "${{ secrets.AZURE_SUBSCRIPTION_ID }}",
            "tenantId": "${{ secrets.AZURE_TENANT_ID }}"
          }

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Azure Container Registry
      run: |
        az acr login --name reliabilitydemoacr${{ env.ACR_ENVIRONMENT }}

    - name: Pull staging images and push to production
      run: |
        VERSION=${{ steps.version.outputs.VERSION }}
        COMMIT_SHA=${{ steps.commit.outputs.COMMIT_SHA }}
        SHORT_SHA=${COMMIT_SHA:0:7}
        
        # Pull staging images built from this commit
        echo "Pulling staging web image..."
        docker pull ${{ env.ACR_REGISTRY }}/${{ env.STAGING_WEB_IMAGE_NAME }}:$SHORT_SHA
        
        echo "Pulling staging worker image..."
        docker pull ${{ env.ACR_REGISTRY }}/${{ env.STAGING_WORKER_IMAGE_NAME }}:$SHORT_SHA
        
        # Tag for production
        echo "Tagging images for production..."
        docker tag ${{ env.ACR_REGISTRY }}/${{ env.STAGING_WEB_IMAGE_NAME }}:$SHORT_SHA ${{ env.ACR_REGISTRY }}/${{ env.PRODUCTION_WEB_IMAGE_NAME }}:v$VERSION
        docker tag ${{ env.ACR_REGISTRY }}/${{ env.STAGING_WEB_IMAGE_NAME }}:$SHORT_SHA ${{ env.ACR_REGISTRY }}/${{ env.PRODUCTION_WEB_IMAGE_NAME }}:latest
        
        docker tag ${{ env.ACR_REGISTRY }}/${{ env.STAGING_WORKER_IMAGE_NAME }}:$SHORT_SHA ${{ env.ACR_REGISTRY }}/${{ env.PRODUCTION_WORKER_IMAGE_NAME }}:v$VERSION
        docker tag ${{ env.ACR_REGISTRY }}/${{ env.STAGING_WORKER_IMAGE_NAME }}:$SHORT_SHA ${{ env.ACR_REGISTRY }}/${{ env.PRODUCTION_WORKER_IMAGE_NAME }}:latest
        
        # Push production images
        echo "Pushing production web images..."
        docker push ${{ env.ACR_REGISTRY }}/${{ env.PRODUCTION_WEB_IMAGE_NAME }}:v$VERSION
        docker push ${{ env.ACR_REGISTRY }}/${{ env.PRODUCTION_WEB_IMAGE_NAME }}:latest
        
        echo "Pushing production worker images..."
        docker push ${{ env.ACR_REGISTRY }}/${{ env.PRODUCTION_WORKER_IMAGE_NAME }}:v$VERSION
        docker push ${{ env.ACR_REGISTRY }}/${{ env.PRODUCTION_WORKER_IMAGE_NAME }}:latest
        
        echo "✅ Successfully promoted staging images to production"

    - name: Update Helm chart versions
      run: |
        VERSION=${{ steps.version.outputs.VERSION }}
        
        # Update image registry domain in values.yaml
        sed -i "s|domain: \".*\"|domain: \"${{ env.ACR_REGISTRY }}\"|g" helm/app/values.yaml
        
        # Update image tags in values.yaml
        sed -i "s|tag: \".*\"|tag: \"v$VERSION\"|g" helm/app/values.yaml
        
        # Update chart version and appVersion in Chart.yaml
        sed -i "s|version: .*|version: $VERSION|g" helm/app/Chart.yaml
        sed -i "s|appVersion: \".*\"|appVersion: \"v$VERSION\"|g" helm/app/Chart.yaml
        
        echo "Updated Helm chart to version $VERSION"

    - name: Configure Git
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"

    - name: Commit and push version updates
      run: |
        VERSION=${{ steps.version.outputs.VERSION }}
        
        # Check if there are changes to commit
        if git diff --quiet; then
          echo "No changes to commit"
          exit 0
        fi
        
        # Add and commit changes
        git add helm/app/values.yaml helm/app/Chart.yaml
        git commit -m "🚀 Release v$VERSION - Update Helm chart versions

        - Updated image registry to ${{ env.ACR_REGISTRY }}
        - Updated web image tag to v$VERSION
        - Updated worker image tag to v$VERSION  
        - Updated chart version to $VERSION
        - Updated appVersion to v$VERSION
        
        Images:
        - ${{ env.ACR_REGISTRY }}/${{ env.PRODUCTION_WEB_IMAGE_NAME }}:v$VERSION
        - ${{ env.ACR_REGISTRY }}/${{ env.PRODUCTION_WORKER_IMAGE_NAME }}:v$VERSION"
        
        # Push changes
        git push origin main
        
        echo "✅ Pushed version update commit to main branch"
        echo "🎯 ArgoCD will now detect changes and deploy v$VERSION"

    - name: Create GitHub Release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref_name }}
        release_name: Release ${{ github.ref_name }}
        body: |
          ## 🚀 Release ${{ github.ref_name }}
          
          ### Container Images
          - **Web API**: `${{ env.ACR_REGISTRY }}/${{ env.PRODUCTION_WEB_IMAGE_NAME }}:v${{ steps.version.outputs.VERSION }}`
          - **Worker**: `${{ env.ACR_REGISTRY }}/${{ env.PRODUCTION_WORKER_IMAGE_NAME }}:v${{ steps.version.outputs.VERSION }}`
          
          ### Deployment
          This release has been automatically deployed via GitOps:
          1. ✅ Staging images promoted to production in ACR
          2. ✅ Helm chart updated with new version
          3. ✅ Changes committed to main branch
          4. 🔄 ArgoCD will detect changes and deploy automatically
          
          ### Architecture
          - Multi-platform images (AMD64 + ARM64)
          - Production-ready Helm chart
          - Automatic scaling and health checks
          - Self-healing deployment via ArgoCD
          
          ### Monitoring
          Monitor the deployment in ArgoCD dashboard or using:
          ```bash
          kubectl get pods -n reliability-demo -w
          ```
        draft: false
        prerelease: false

    - name: Deployment Summary
      run: |
        VERSION=${{ steps.version.outputs.VERSION }}
        echo "## 🎉 Release v$VERSION Completed!"
        echo ""
        echo "### What happened:"
        echo "1. ✅ Promoted staging images to production in ACR"
        echo "   - ${{ env.ACR_REGISTRY }}/${{ env.PRODUCTION_WEB_IMAGE_NAME }}:v$VERSION"
        echo "   - ${{ env.ACR_REGISTRY }}/${{ env.PRODUCTION_WORKER_IMAGE_NAME }}:v$VERSION"
        echo ""
        echo "2. ✅ Updated Helm chart with new version"
        echo "3. ✅ Committed changes to main branch"
        echo "4. ✅ Created GitHub release"
        echo ""
        echo "### Next steps:"
        echo "🔄 ArgoCD will automatically detect the changes and deploy v$VERSION"
        echo "📊 Monitor deployment in ArgoCD dashboard"
        echo "🔍 Check application health at /api/health endpoint"
        echo ""
        echo "### Rollback (if needed):"
        echo "git revert HEAD && git push"